{% extends '@App/Layout/layout_.html.twig' %}

{% block content %}

    <!-- Header -->
    <header class="w3-container w3-theme w3-padding" id="myHeader">
        <i onclick="w3_open()" class="fa fa-bars w3-xlarge w3-button w3-theme"></i>
        <div class="w3-center">
            <h4>Skills</h4>
        </div>
    </header>

    <div class="w3-padding w3-white w3-display-container">
        <br>

        <h1>New Skill</h1>

        <div class="w3-responsive w3-card-4">
            {{ form_start(skillsForm) }}
            <table class="w3-table w3-striped w3-bordered">
                <tr>
                    <td>
                        <b>{{ form_label(skillsForm.description) }}</b><br>
                        {{ form_errors(skillsForm.description) }}
                        {{ form_widget(skillsForm.description) }}
                    </td>
                </tr>
                <tr>
                    <td>
                        <b>{{ form_label(skillsForm.competency) }}</b><br>
                        {{ form_errors(skillsForm.competency) }}
                        {{ form_widget(skillsForm.competency) }}
                    </td>
                </tr>
            </table>
            <button type="submit" class="w3-button w3-theme">Save</button>
            {{ form_end(skillsForm) }}
        </div>
    </div>

{% endblock %}